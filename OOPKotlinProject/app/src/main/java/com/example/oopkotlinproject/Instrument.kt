package com.example.oopkotlinproject

interface Instrument {

    // Java -> function da  body olamıyor override edilmesi zorunlu
    // Kotlin -> function da body isterse olur ister olmaz
    //Function
   fun info()
}